# UI → API Mapping — ISKCON Portal

**Generated:** 2026-04-18  
**Tech Stack:** React 18 + Vite + TypeScript (Frontend) · Node.js + Express + MongoDB (Backend)  
**Base URL:** `http://localhost:5000/api/v1` (via `VITE_API_BASE_URL`)  
**Auth:** Bearer token in `Authorization` header (read from Zustand `iskcon-auth` store in localStorage)

---

## Legend

| Symbol | Meaning |
|--------|---------|
| ⚠️ MISMATCH | Frontend calls an endpoint that does NOT exist in the backend |
| 🔴 NO API | UI control has no backend API call (simulated / mock data / local state only) |
| ✅ Connected | UI control maps correctly to a working backend endpoint |

---

## Table of Contents

1. [Auth Module](#1-auth-module)
2. [Student Module](#2-student-module)
3. [Admin Module](#3-admin-module)
4. [Examiner Module](#4-examiner-module)
5. [Public Module](#5-public-module)
6. [Temple Registration Module](#6-temple-registration-module)

---

## 1. Auth Module

| Module Name | UI Screen | Operation | API Call (Service Fn) | Endpoint URL | Sample Request | Sample Response JSON | Business Logic | DB Collection | DB Operation | Remarks |
|---|---|---|---|---|---|---|---|---|---|---|
| Auth | LoginPage | "Sign In" button submit | `loginUser(email, password)` | `POST /auth/login` | `{ "email": "student@iskcon.org", "password": "Pass@1234" }` | `{ "success": true, "message": "Login successful", "data": { "accessToken": "eyJ...", "user": { "_id": "...", "name": "Ravi Kumar", "email": "student@iskcon.org", "role": "student", "studentId": "PIHE-2024-0001" } } }` | Validates bcrypt password; issues JWT access token (15 min) + sets refresh token as httpOnly cookie; redirects based on role (`/student/dashboard`, `/admin/dashboard`, `/examiner/dashboard`) | `users` | `findOne` (by email), `save` (update refreshToken) | ✅ Connected. Zod validation: email + password (min 1). |
| Auth | LoginPage | "Continue with Google" link | Browser redirect (no Axios call) | `GET /auth/google` (Passport.js redirect) | N/A — browser navigation | Redirects to Google OAuth consent screen | Initiates Google OAuth 2.0 flow via Passport.js; no frontend fetch | N/A | N/A | ✅ Connected. URL built as `{VITE_API_BASE_URL}/auth/google`. |
| Auth | RegisterPage | "Create Account" button submit | `registerUser(name, email, phone, password)` | `POST /auth/register` | `{ "name": "Ravi Kumar", "email": "ravi@example.com", "phone": "9876543210", "password": "Pass@1234" }` | `{ "success": true, "message": "Registration successful", "data": { "accessToken": "eyJ...", "user": { "_id": "...", "name": "Ravi Kumar", "role": "student", "studentId": "PIHE-2024-0001" } } }` | Creates user with bcrypt hash (12 rounds); auto-generates `studentId` (format: PIHE-YYYY-NNNN); issues tokens; shows student ID in modal post-registration; navigates to `/student/dashboard` | `users`, `counters` | `create`, `findOneAndUpdate` ($inc on counter) | ✅ Connected. `confirmPassword` stripped before API call. Zod: name ≥ 2 chars, email valid, phone optional (10-digit), password ≥ 8 chars with upper/lower/digit. |
| Auth | RegisterPage | "Continue with Google" link | Browser redirect (no Axios call) | `GET /auth/google` | N/A | Redirects to Google OAuth consent | Same as Login Google OAuth flow | N/A | N/A | ✅ Connected. |
| Auth | OAuthCallbackPage | Page load (useEffect) | `getProfile()` | `GET /auth/me` | Headers: `Authorization: Bearer <token_from_url_param>` | `{ "success": true, "data": { "user": { "_id": "...", "name": "Ravi Kumar", "email": "ravi@google.com", "role": "student" } } }` | Extracts `token` and `role` from URL query params (`?token=...&role=...`); stores in Zustand; calls `getProfile()` to hydrate full user object; redirects to role-based dashboard | `users` | `findById` | ✅ Connected. Token injected into Axios instance before `getProfile()` call. |
| Auth | ForgotPasswordPage | "Send Reset Link" button submit | `setTimeout` simulation only | `POST /auth/forgot-password` (in authService but NOT called by this page) | `{ "email": "user@example.com" }` | N/A | Uses `setTimeout` to simulate API call. Comment in code: "API call would go here". `forgotPassword()` in authService.ts calls `/auth/forgot-password` but this page ignores it. | N/A | N/A | 🔴 NO API + ⚠️ MISMATCH. Page is fully simulated. `authService.forgotPassword` calls a non-existent backend route `/auth/forgot-password`. |
| Auth | All Layouts (Admin/Student/Examiner sidebar) | Logout button click | `logoutUser()` | `POST /auth/logout` | `{}` (withCredentials: true — sends refresh token cookie) | `{ "success": true, "message": "Logged out successfully" }` | Calls backend to clear refreshToken in DB; clears httpOnly cookie; clears Zustand store; navigates to `/login` | `users` | `findByIdAndUpdate` (set refreshToken: null) | ✅ Connected. Defined in `AdminLayout.tsx`, `StudentLayout.tsx`, `ExaminerLayout.tsx`. |
| Auth | Axios interceptor | Silent token refresh on 401 | `refreshAccessToken()` | `POST /auth/refresh` | `{}` (withCredentials: true — sends httpOnly refresh cookie) | `{ "success": true, "data": { "accessToken": "eyJ..." } }` | On any 401 response, interceptor queues concurrent requests and calls `/auth/refresh`; on success, replays all queued requests with new token; on failure, clears store and redirects to `/login` | `users` | `findOne` (by refreshToken), `save` (update refreshToken) | ✅ Connected. Implemented in `src/services/api.ts`. Prevents multiple simultaneous refresh calls. |

---

## 2. Student Module

| Module Name | UI Screen | Operation | API Call (Service Fn) | Endpoint URL | Sample Request | Sample Response JSON | Business Logic | DB Collection | DB Operation | Remarks |
|---|---|---|---|---|---|---|---|---|---|---|
| Student | DashboardPage | Page load (useEffect) | `getMyEnrollments()` | `GET /enrollments/my` | Headers: `Authorization: Bearer <token>` | `{ "success": true, "data": { "enrollments": [ { "_id": "...", "course": { "_id": "...", "title": "Intro to Bhagavad Gita" }, "progress": 40, "completedLessons": ["..."], "enrolledAt": "2024-01-15T..." } ] } }` | Fetches student's active enrollments; populates course title; used to show enrolled course cards and calculate active vs completed | `enrollments` | `find` (by student), `populate` (course) | ✅ Connected. Uses `Promise.allSettled` — fails gracefully. Falls back to `MOCK_ENROLLMENTS` on error. |
| Student | DashboardPage | Page load (useEffect) | `getMyResults()` | `GET /results/my` | Headers: `Authorization: Bearer <token>` | `{ "success": true, "data": { "results": [ { "_id": "...", "exam": { "title": "Gita Final" }, "marksObtained": 75, "totalMarks": 100, "percentage": 75, "isPassed": true, "approvedByAdmin": true } ] } }` | Fetches evaluated/approved results; used to show pass/fail status on dashboard | `results` | `find` (by student), `populate` (exam, submission) | ✅ Connected. Falls back to `MOCK_RESULTS` on error. |
| Student | DashboardPage | Page load (useEffect) | `getMyCertificates()` | `GET /certificates/my` | Headers: `Authorization: Bearer <token>` | `{ "success": true, "data": { "certificates": [ { "_id": "...", "courseTitle": "Intro to Bhagavad Gita", "certId": "uuid-v4-here", "issuedAt": "2024-02-01T...", "pdfUrl": "./generated-certs/cert-uuid.pdf" } ] } }` | Fetches issued certificates; used to show certificate count on dashboard stats | `certificates` | `find` (by student), `populate` (course) | ✅ Connected. Falls back to empty array on error. |
| Student | MyCoursesPage | Page load (useEffect) | `getMyEnrollments()` | `GET /enrollments/my` | Headers: `Authorization: Bearer <token>` | Same as DashboardPage enrollment response | Fetches enrollments; "Active" / "Completed" tab switching is client-side filter based on `progress === 100` | `enrollments` | `find` (by student) | ✅ Connected. Tab filtering is purely client-side. |
| Student | CoursePageStudent | Page load (useEffect) | `getCourseById(courseId)` | `GET /courses/:id` | Path: `/courses/64abc123` · Headers: `Authorization: Bearer <token>` | `{ "success": true, "data": { "course": { "_id": "...", "title": "Bhagavad Gita", "lessons": [ { "_id": "...", "title": "Chapter 1", "content": "...", "videoUrl": "...", "isPublished": true } ], "lessonCount": 10 } } }` | Fetches full course with lessons array; enrollment info overlaid from Zustand store | `courses` | `findById` | ✅ Connected. |
| Student | CoursePageStudent | "Mark as Complete" button click | `updateLessonProgress(enrollmentId, lessonId)` | `PATCH /enrollments/:id/progress` | Path: `/enrollments/64abc456/progress` · Body: `{ "lessonId": "64lesson789" }` | `{ "success": true, "data": { "enrollment": { "completedLessons": ["64lesson789"], "progress": 50 } } }` | Adds lessonId to `completedLessons`; recalculates `progress` as percentage; uses **optimistic update** — updates UI before API confirms; reverts on error | `enrollments` | `findOneAndUpdate` ($addToSet on completedLessons, $set on progress) | ✅ Connected. Optimistic UI update pattern. |
| Student | CoursePageStudent | File drop / "Browse" file upload | `uploadOfflinePaper('', courseId, file)` | `POST /submissions/upload` | `multipart/form-data`: `examId=''`, `courseId=64abc123`, `paper=<File>` | `{ "success": true, "data": { "submission": { "_id": "...", "type": "offline", "uploadedFile": "uploads/submissions/paper-uuid.pdf", "status": "pending" } } }` | Uploads PDF answer paper; creates Submission record with type=offline; file stored by Multer in `uploads/submissions/` | `submissions` | `create` | ✅ Connected. Drag-and-drop or file browse triggers upload. |
| Student | CoursePageStudent | "Take Online Exam" button | `<Link to="/student/courses/:id/exam">` — no API call | N/A | N/A | N/A | Client-side navigation only | N/A | N/A | 🔴 NO API. Just a React Router Link. |
| Student | ExamSectionPage | Page load (useEffect) | `getExamByCourse(courseId)` | `GET /exams/course/:courseId` | Path: `/exams/course/64abc123` · Headers: `Authorization: Bearer <token>` | `{ "success": true, "data": { "exam": { "_id": "...", "title": "Gita Final Exam", "totalMarks": 100, "passingMarks": 40, "duration": 60, "questions": [ { "_id": "...", "questionText": "What is Dharma?", "type": "mcq", "options": ["A", "B", "C", "D"], "marks": 2 } ] } } }` | Fetches exam for the course; `correctAnswer` is stripped server-side (never sent to frontend); exam timer starts after load | `exams` | `findOne` (by course) | ✅ Connected. correctAnswer intentionally excluded from response. |
| Student | ExamSectionPage | "Submit Now" button (in ConfirmModal) | `submitExam({ examId, answers })` | `POST /submissions` | `{ "examId": "64exam001", "answers": [ { "questionId": "64q001", "selectedOption": "B" }, { "questionId": "64q002", "answerText": "Karma is action" } ] }` | `{ "success": true, "data": { "submission": { "_id": "...", "status": "pending", "type": "online", "submittedAt": "2024-01-20T..." } } }` | Creates online Submission with MCQ/short answers; associates with current student and exam; pending examiner assignment | `submissions` | `create` | ✅ Connected. Same handler used for both manual submit and timer expiry. |
| Student | ExamSectionPage | Timer expiry (auto-submit) | `submitExam({ examId, answers })` | `POST /submissions` | Same as above with partial answers | Same as above | Auto-submits with whatever answers are recorded when countdown reaches zero | `submissions` | `create` | ✅ Connected. No separate endpoint — reuses `handleSubmit()`. |
| Student | ResultsPage | Page load (useEffect) | `getMyResults()` | `GET /results/my` | Headers: `Authorization: Bearer <token>` | `{ "success": true, "data": { "results": [ { "_id": "...", "marksObtained": 75, "totalMarks": 100, "percentage": 75, "isPassed": true, "approvedByAdmin": true, "evaluatedAt": "2024-01-25T..." } ] } }` | Displays evaluated and admin-approved results; no interactive controls | `results` | `find` (by student), `populate` (exam, submission) | ✅ Connected. Falls back to `MOCK_RESULTS` array if API fails. |
| Student | CertificatesPage | Page load (useEffect) | `getMyCertificates()` | `GET /certificates/my` | Headers: `Authorization: Bearer <token>` | `{ "success": true, "data": { "certificates": [ { "_id": "...", "courseTitle": "Bhagavad Gita", "certId": "uuid-here", "issuedAt": "2024-02-01T...", "pdfUrl": "./generated-certs/cert-uuid.pdf" } ] } }` | Fetches list of certificates issued to logged-in student | `certificates` | `find` (by student), `populate` (course) | ✅ Connected. |
| Student | CertificatesPage | "Download PDF" button click | `downloadCertificate(certId)` | `GET /certificates/:certId/download` | Path: `/certificates/uuid-here/download` · `responseType: 'blob'` | Binary PDF blob | Fetches PDF binary; creates `<a>` element with `blob:` URL; triggers browser download as `certificate-{certId}.pdf`; revokes blob URL after download | `certificates` | `findOne` (by certId + student), reads file from disk | ✅ Connected. Security: studentId verified — can only download own certs. |
| Student | CertificatesPage | "Verify" button click | `window.open('/verify/${certId}', '_blank')` | Client-side navigation only | N/A | N/A | Opens local verify route in new tab; no direct API call from this component | N/A | N/A | 🔴 NO API at component level. Navigates to public verify page which calls `GET /certificates/verify/:certId`. |
| Student | VerifyCertificate (public route) | Page load | `verifyCertificate(certId)` | `GET /certificates/verify/:certId` | Path: `/certificates/verify/uuid-here` | `{ "valid": true, "certId": "uuid-here", "studentName": "Ravi Kumar", "studentId": "PIHE-2024-0001", "course": "Bhagavad Gita", "issuedAt": "2024-02-01T..." }` | Public endpoint — no auth; verifies certificate authenticity by UUID; used for QR code / link verification | `certificates` | `findOne` (by certificateId), `populate` (student, course) | ✅ Connected. No authentication required. |

---

## 3. Admin Module

| Module Name | UI Screen | Operation | API Call (Service Fn) | Endpoint URL | Sample Request | Sample Response JSON | Business Logic | DB Collection | DB Operation | Remarks |
|---|---|---|---|---|---|---|---|---|---|---|
| Admin | DashboardPage | Page load (useEffect) | `getDashboard()` | `GET /admin/dashboard` | Headers: `Authorization: Bearer <token>` | `{ "success": true, "data": { "kpis": { "totalStudents": 250, "totalExaminers": 10, "totalCourses": 8, "totalEnrollments": 430, "pendingSubmissions": 12, "pendingApprovals": 5, "totalCertificates": 85 }, "monthlyEnrollments": [ { "_id": { "year": 2024, "month": 1 }, "count": 45 } ], "recentEnrollments": [ { "_id": "...", "student": { "name": "Ravi", "studentId": "PIHE-2024-0001" }, "course": { "title": "Gita" }, "enrolledAt": "2024-01-10T..." } ] } }` | MongoDB aggregation pipeline: counts users by role, counts enrollments/submissions/results; monthly trend via `$group` by year+month; 5 most recent enrollments | `users`, `enrollments`, `submissions`, `results`, `certificates` | `aggregate` (multiple pipelines), `find` (recent enrollments) | ✅ Connected. Charts rendered with Recharts (LineChart, PieChart). |
| Admin | StudentManagementPage | Page load / search (useEffect + debounce) | `getStudents({ page, limit, search })` | `GET /admin/students` | Query: `?page=1&limit=20&search=ravi` | `{ "success": true, "data": { "students": [ { "_id": "...", "name": "Ravi Kumar", "email": "ravi@example.com", "studentId": "PIHE-2024-0001", "isActive": true, "createdAt": "2024-01-01T..." } ], "total": 250 } }` | Server-side pagination (limit=20); regex search across name/email/studentId; returns only role=student users | `users` | `find` (with regex + pagination), `countDocuments` | ✅ Connected. 400ms debounce on search input. |
| Admin | StudentManagementPage | Toggle (activate/deactivate) icon click → Confirm modal → Confirm | `toggleStudentStatus(studentId)` | `PATCH /admin/students/:id/toggle` | Path: `/admin/students/64abc001/toggle` | `{ "success": true, "data": { "user": { "_id": "...", "isActive": false } } }` | Flips `isActive` boolean; if deactivating, also nullifies `refreshToken` to force logout | `users` | `findByIdAndUpdate` ($set isActive, optionally $unset refreshToken) | ✅ Connected. Optimistic UI update: local state updated immediately after API confirms. |
| Admin | StudentManagementPage | "Export CSV" button | `exportCSV('students')` | `GET /admin/export?type=students` | Query: `?type=students` · `responseType: 'blob'` | CSV file blob | Generates CSV of all students via `csv-stringify`; creates download link in browser | `users` | `find` (role=student, all records) | ✅ Connected. |
| Admin | ExaminerManagementPage | Page load / search | `getExaminers({ search })` | `GET /admin/examiners` | Query: `?search=prabhu` | `{ "success": true, "data": [ { "_id": "...", "name": "Prabhu Das", "email": "prabhu@iskcon.org", "isActive": true } ] }` | Fetches all examiners; supports optional regex search on name/email | `users` | `find` (role=examiner) | ✅ Connected. |
| Admin | ExaminerManagementPage | "Add Examiner" button → form modal → "Create Examiner" submit | `createExaminer({ name, email, password, phone })` | `POST /admin/examiners` | `{ "name": "Prabhu Das", "email": "prabhu@iskcon.org", "password": "Secure@123", "phone": "9876543210" }` | `{ "success": true, "data": { "_id": "...", "name": "Prabhu Das", "email": "prabhu@iskcon.org", "role": "examiner", "isActive": true } }` | Creates user with role=examiner; password bcrypt-hashed; returns new examiner object to prepend to list | `users` | `create` | ✅ Connected. Password validated (min 8 chars) on frontend. |
| Admin | ExaminerManagementPage | Toggle icon click | `toggleExaminerStatus(examinerId)` | `PATCH /admin/examiners/:id/toggle` | Path: `/admin/examiners/64abc002/toggle` | `{ "success": true, "data": { "user": { "_id": "...", "isActive": false } } }` | Flips `isActive`; deactivation nullifies refresh token | `users` | `findByIdAndUpdate` | ✅ Connected. |
| Admin | CourseManagementPage | Page load | `adminGetCourses({ limit: 100 })` | `GET /courses` | Query: `?limit=100` · Headers: `Authorization: Bearer <token>` | `{ "success": true, "data": { "courses": [ { "_id": "...", "title": "Bhagavad Gita", "category": "Scriptures", "level": "Beginner", "isPublished": true, "isFeatured": false, "lessons": [...], "createdAt": "..." } ], "total": 8 } }` | Fetches all courses with lesson arrays; client-side filter/search | `courses` | `find` (with pagination) | ✅ Connected. Client-side text filter on title/category. |
| Admin | CourseManagementPage | "Edit" icon → navigates to `/admin/courses/:id/edit` | Navigation only | N/A | N/A | N/A | React Router navigation to edit page | N/A | N/A | 🔴 NO API at click. Edit page uses `adminUpdateCourse()`. |
| Admin | CourseManagementPage | Publish/Unpublish toggle icon → Confirm modal → Confirm | `adminUpdateCourse(courseId, { isPublished: !current })` | `PUT /courses/:id` | `{ "isPublished": true }` | `{ "success": true, "data": { "course": { "_id": "...", "isPublished": true } } }` | Toggles course visibility; published courses appear to students | `courses` | `findByIdAndUpdate` | ✅ Connected. |
| Admin | CourseManagementPage | Star (Featured) icon → Confirm modal → Confirm | `adminUpdateCourse(courseId, { isFeatured: !current })` | `PUT /courses/:id` | `{ "isFeatured": true }` | `{ "success": true, "data": { "course": { "_id": "...", "isFeatured": true } } }` | Toggles featured flag; featured courses shown prominently on homepage | `courses` | `findByIdAndUpdate` | ✅ Connected. |
| Admin | CourseManagementPage | "Delete" icon → Confirm modal → Confirm | `adminDeleteCourse(courseId)` | `DELETE /courses/:id` | Path: `/courses/64abc123` | `{ "success": true, "message": "Course deleted" }` | Deletes course; associated enrollments/exams may become orphaned | `courses` | `findByIdAndDelete` | ✅ Connected. |
| Admin | AddCoursePage | "Create Course" button submit | `adminCreateCourse(formData)` | `POST /courses` | `multipart/form-data`: title, description, category, level, isPaid, price, isFeatured, isPublished, tags, lessons (JSON string) | `{ "success": true, "data": { "course": { "_id": "...", "title": "Intro to Yoga", "lessons": [...] } } }` | Creates new course with embedded lessons array; lessons filtered to exclude untitled; navigates to `/admin/courses` on success | `courses` | `create` | ✅ Connected. Lessons serialized as JSON string in FormData. |
| Admin | ExamManagementPage | Page load | `adminGetExams()` | `GET /exams` | Headers: `Authorization: Bearer <token>` | Expected: `{ "exams": [...] }` | Fetch all exams for admin listing | `exams` | `find` | ⚠️ MISMATCH. `GET /exams` route does NOT exist in backend routes. Only `GET /exams/course/:courseId` exists. |
| Admin | ExamManagementPage | "Create Exam" button → modal → "Create Exam" submit | `adminCreateExam({ title, passingMarks, duration, totalMarks, questions })` | `POST /exams` | `{ "title": "Gita Final", "passingMarks": 40, "duration": 60, "totalMarks": 10, "questions": [ { "questionText": "What is Dharma?", "type": "multiple-choice", "options": ["A","B","C","D"], "correctAnswer": "B", "marks": 2 } ] }` | Expected: `{ "exam": { "_id": "...", ... } }` | Creates exam without course association (no courseId in form) | `exams` | `create` | ⚠️ MISMATCH. `POST /exams` route does NOT exist in backend. Existing exam creation is only via `examController` triggered by course routes. |
| Admin | ExamManagementPage | Delete icon (with browser `confirm()` dialog) | `adminDeleteExam(examId)` | `DELETE /exams/:id` | Path: `/exams/64exam001` | Expected: `{ "message": "Exam deleted" }` | Deletes exam; removes from list | `exams` | `findByIdAndDelete` | ⚠️ MISMATCH. `DELETE /exams/:id` route does NOT exist in backend. |
| Admin | CertificateManagementPage (Pending tab) | Page load | `getPendingResults({ page, limit })` | `GET /admin/results/pending` | Query: `?page=1&limit=15` | `{ "success": true, "data": { "results": [ { "_id": "...", "student": { "name": "Ravi", "studentId": "PIHE-2024-0001" }, "exam": { "title": "Gita Final", "totalMarks": 100 }, "marksObtained": 75, "percentage": 75 } ], "total": 5 } }` | Fetches evaluated results awaiting admin approval; `approvedByAdmin` is false | `results` | `find` (approvedByAdmin: false, status: evaluated), `populate` | ✅ Connected. |
| Admin | CertificateManagementPage (Pending tab) | "Approve" button → Confirm modal → "Approve & Generate Certificate" | `approveResult(resultId)` | `PATCH /admin/results/:id/approve` | Path: `/admin/results/64result001/approve` | `{ "success": true, "data": { "result": { "approvedByAdmin": true }, "certificate": { "_id": "...", "certificateId": "uuid-here" } } }` | Sets `approvedByAdmin = true`; triggers PDFKit certificate generation (A4 landscape, ISKCON branding, QR verify URL); saves PDF to `./generated-certs/`; creates Certificate record | `results`, `certificates` | `findByIdAndUpdate` (result), `create` (certificate), PDF written to disk | ✅ Connected. Certificate auto-generated on approval. |
| Admin | CertificateManagementPage (All tab) | Page load | `getAllCertificates({ page, limit })` | `GET /admin/certificates` | Query: `?page=1&limit=15` | `{ "success": true, "data": { "certificates": [ { "_id": "...", "student": { "name": "Ravi", "studentId": "PIHE-2024-0001" }, "course": { "title": "Gita" }, "certificateId": "uuid-here", "issuedAt": "2024-02-01T..." } ], "total": 85 } }` | Lists all issued certificates with student and course info | `certificates` | `find` (all), `populate` (student, course) | ✅ Connected. Client-side search filter on name/studentId/certId. |
| Admin | CertificateManagementPage (All tab) | "Export CSV" button | `exportCSV('certificates')` | `GET /admin/export?type=certificates` | Query: `?type=certificates` | CSV blob | CSV export of all certificates | `certificates` | `find` (all) | ✅ Connected. |
| Admin | ReportsPage | Page load | `getReports()` | `GET /admin/reports` | Headers: `Authorization: Bearer <token>` | `{ "success": true, "data": { "enrollmentsByMonth": [...], "resultsByStatus": [{"_id": true, "count": 80}, {"_id": false, "count": 20}], "topCourses": [{"title": "Gita", "count": 120}], "examinerStats": [{"name": "Prabhu", "total": 30, "evaluated": 28}] } }` | Aggregation pipeline returning 4 datasets: monthly enrollment trend, pass/fail distribution, top 5 courses by enrollment, examiner workload stats | `enrollments`, `results`, `users` | `aggregate` (multiple complex pipelines) | ✅ Connected. Charts: LineChart, PieChart, BarChart (via Recharts). |
| Admin | ReportsPage | "Export Students CSV" button | `exportCSV('students')` | `GET /admin/export?type=students` | Query: `?type=students` | CSV blob | Downloads student list as CSV | `users` | `find` (role=student) | ✅ Connected. |
| Admin | ReportsPage | "Export Results CSV" button | `exportCSV('results')` | `GET /admin/export?type=results` | Query: `?type=results` | CSV blob | Downloads results as CSV | `results` | `find` (all), `populate` | ✅ Connected. |
| Admin | ReportsPage | "Export Certificates CSV" button | `exportCSV('certificates')` | `GET /admin/export?type=certificates` | Query: `?type=certificates` | CSV blob | Downloads certificates as CSV | `certificates` | `find` (all), `populate` | ✅ Connected. |
| Admin | Admin — Submission Management | Page load (via adminService) | `getAllSubmissions()` | `GET /admin/submissions` | Query: optional `?status=pending` | `{ "success": true, "data": { "submissions": [ { "_id": "...", "student": { "name": "Ravi" }, "exam": { "title": "Gita" }, "type": "online", "status": "pending" } ] } }` | Fetches all submissions across all students | `submissions` | `find` (all), `populate` | ✅ Connected. No dedicated admin submissions page found — accessible via adminService. |
| Admin | Admin — Submission Assignment | Assign button (via adminService) | `assignSubmission(submissionId, examinerId)` | `PATCH /admin/submissions/:id/assign` | `{ "examinerId": "64examiner001" }` | `{ "success": true, "data": { "submission": { "_id": "...", "assignedTo": "64examiner001", "status": "pending" } } }` | Assigns submission to an examiner for evaluation | `submissions` | `findByIdAndUpdate` | ✅ Connected. No dedicated page — accessible via adminService. |
| Admin | Admin — All Results | Page load (via adminService) | `getAllResults()` | `GET /admin/results` | Headers: `Authorization: Bearer <token>` | `{ "success": true, "data": { "results": [...] } }` | Fetches all results across all students | `results` | `find` (all), `populate` | ✅ Connected. No dedicated page found. |

---

## 4. Examiner Module

| Module Name | UI Screen | Operation | API Call (Service Fn) | Endpoint URL | Sample Request | Sample Response JSON | Business Logic | DB Collection | DB Operation | Remarks |
|---|---|---|---|---|---|---|---|---|---|---|
| Examiner | DashboardPage | Page load (useEffect) | `getAssignedSubmissions()` | `GET /submissions/assigned` | Headers: `Authorization: Bearer <token>` | `{ "success": true, "data": [ { "_id": "...", "student": { "name": "Ravi Kumar", "studentId": "PIHE-2024-0001" }, "course": { "_id": "...", "title": "Bhagavad Gita" }, "exam": { "title": "Gita Final" }, "type": "online", "status": "pending", "submittedAt": "2024-01-20T..." } ] }` | Fetches all submissions assigned to the logged-in examiner (by `assignedTo` field); used to derive pending/completed counts and show 5 most recent | `submissions` | `find` (assignedTo: req.user._id), `populate` (student, course, exam) | ✅ Connected. Stats (pending/completed count) computed client-side. |
| Examiner | DashboardPage | "View All Students" link | React Router Link to `/examiner/students` | N/A | N/A | N/A | Navigation only | N/A | N/A | 🔴 NO API. |
| Examiner | DashboardPage | "Evaluate" / "View" row link | React Router Link to `/examiner/evaluate/:submissionId` | N/A | N/A | N/A | Navigation only | N/A | N/A | 🔴 NO API. |
| Examiner | AssignedStudentsPage | Page load (useEffect) | `getAssignedSubmissions()` | `GET /submissions/assigned` | Headers: `Authorization: Bearer <token>` | Same as Examiner DashboardPage response | Full list of assigned submissions; status/course/search filters applied client-side | `submissions` | `find` (assignedTo: examiner) | ✅ Connected. All filtering (search, status, course) is client-side on loaded data. |
| Examiner | EvaluateExamPage | Page load (useEffect) | `getSubmissionById(submissionId)` | `GET /submissions/:id` | Path: `/submissions/64sub001` · Headers: `Authorization: Bearer <token>` | `{ "success": true, "data": { "_id": "...", "student": { "name": "Ravi", "studentId": "PIHE-2024-0001" }, "exam": { "_id": "...", "title": "Gita Final", "totalMarks": 100, "passingMarks": 40, "questions": [ { "_id": "...", "questionText": "...", "type": "mcq", "options": [...], "correctAnswer": "B", "marks": 2 } ] }, "answers": [ { "questionId": "...", "selectedOption": "B" } ], "type": "online", "status": "pending", "uploadedFile": null } }` | Fetches full submission with exam questions (correctAnswer visible to examiner), student answers, offline file path if applicable | `submissions` | `findById`, `populate` (student, exam with questions, course) | ✅ Connected. correctAnswer IS returned to examiner (unlike student exam fetch). |
| Examiner | EvaluateExamPage | "Submit Evaluation" button → Confirm modal → "Confirm & Submit" | `evaluateSubmission(submissionId, { marksObtained, remarks })` | `PATCH /submissions/:id/evaluate` | `{ "marksObtained": 78, "remarks": "Good understanding of Karma concept." }` | `{ "success": true, "data": { "submission": { "_id": "...", "status": "evaluated", "marksObtained": 78 }, "result": { "_id": "...", "percentage": 78, "isPassed": true } } }` | Sets submission status=evaluated; creates/updates Result record; calculates percentage; result awaits admin approval | `submissions`, `results` | `findByIdAndUpdate` (submission), `create` or `findOneAndUpdate` (result) | ✅ Connected. `canSubmit` validation: all questions must have marks entered, none can exceed question max. |

---

## 5. Public Module

| Module Name | UI Screen | Operation | API Call (Service Fn) | Endpoint URL | Sample Request | Sample Response JSON | Business Logic | DB Collection | DB Operation | Remarks |
|---|---|---|---|---|---|---|---|---|---|---|
| Public | CoursesPage | Page load (useEffect) | `getMockCourses()` — MOCK, no API | N/A | N/A | Hardcoded array of 6 mock course objects | Returns static mock data; no real API call; filter/search/pagination all client-side on mock data | N/A | N/A | 🔴 NO API. Critical mismatch — `getCourses()` service function exists in `courseService.ts` and calls `GET /courses`, but the page intentionally uses `getMockCourses()` instead. |
| Public | CourseDetailPage | Page load (useEffect) | `getMockCourses().find(c => c._id === id)` — MOCK, no API | N/A | N/A | Single mock course object found by ID match | Returns mock course data; no real API call | N/A | N/A | 🔴 NO API. `getCourseById()` service fn exists but is not used here. "Enroll Now →" navigates to `/register`. |
| Public | HomePage | Static render | None | N/A | N/A | N/A | Static marketing page | N/A | N/A | 🔴 NO API. |
| Public | AboutPage | Static render | None | N/A | N/A | N/A | Static page | N/A | N/A | 🔴 NO API. |
| Public | ContactPage | Static render | None | N/A | N/A | N/A | Static page | N/A | N/A | 🔴 NO API. |

---

## 6. Temple Registration Module

> **⚠️ CRITICAL FINDING:** The entire 4-step temple registration UI flow makes **ZERO API calls**. All form state is managed via React Context (`TempleFormContext`). The "Submit Registration" button on the final review step navigates directly to `/temple-registration/success` without calling any backend endpoint. The "Verify Mobile" button sets local state only. The backend has a complete `templeService.js` (470 lines with MongoDB transactions), `templeController.js`, and routes — but they are **not connected** to the frontend.

| Module Name | UI Screen | Operation | API Call (Service Fn) | Endpoint URL | Sample Request | Sample Response JSON | Business Logic | DB Collection | DB Operation | Remarks |
|---|---|---|---|---|---|---|---|---|---|---|
| Temple Registration | TempleDetails (Step 1) | All form inputs (Temple Name, GST, Address, State, City, Pin) | None — updates React Context only | N/A | N/A | N/A | Stores data in `TempleFormContext` via `updateTemple()` | N/A | N/A | 🔴 NO API. Local state only. |
| Temple Registration | TempleDetails (Step 1) | Logo file upload | None — stores base64/URL in React Context | N/A | N/A | N/A | `TempleFileUpload` component converts file to base64 preview and stores in context | N/A | N/A | 🔴 NO API. Backend has `POST /api/v1/upload` with Multer but this page does NOT call it. |
| Temple Registration | TempleDetails (Step 1) | "Next Step →" button | `navigate('/temple-registration/president')` | N/A | N/A | N/A | Updates context with `updateTemple(localData)`; navigates to step 2 | N/A | N/A | 🔴 NO API. |
| Temple Registration | PresidentDetails (Step 2) | All form inputs (Name, Mobile, PAN, Aadhar, Email) | None — updates React Context only | N/A | N/A | N/A | Stores in `TempleFormContext` via `updatePresident()` | N/A | N/A | 🔴 NO API. |
| Temple Registration | PresidentDetails (Step 2) | Photo upload | None — base64 in Context | N/A | N/A | N/A | Same as logo upload pattern | N/A | N/A | 🔴 NO API. Backend has `POST /api/v1/upload` but not used. |
| Temple Registration | PresidentDetails (Step 2) | ID Card upload | None — base64 in Context | N/A | N/A | N/A | Same as photo upload | N/A | N/A | 🔴 NO API. |
| Temple Registration | PresidentDetails (Step 2) | "Verify" suffix button on Mobile field | `toast.success('Mobile number verified successfully!')` only — local state | N/A | N/A | N/A | Sets `mobileVerified: true` in local state if mobile length === 10 (pure client-side validation, no real OTP) | N/A | N/A | 🔴 NO API. Backend has full OTP flow (`POST /api/v1/otp/send`, `POST /api/v1/otp/verify`) — completely disconnected from this UI. |
| Temple Registration | PresidentDetails (Step 2) | "Next Step →" button | `navigate('/temple-registration/vice-president')` | N/A | N/A | N/A | Updates context; navigates to step 3 | N/A | N/A | 🔴 NO API. |
| Temple Registration | VicePresidentDetails (Step 3) | All form inputs + uploads + "Verify" mobile | Same as PresidentDetails | N/A | N/A | N/A | Same pattern as President step | N/A | N/A | 🔴 NO API. |
| Temple Registration | VicePresidentDetails (Step 3) | "Next Step →" button | `navigate('/temple-registration/review')` | N/A | N/A | N/A | Updates VP context; navigates to step 4 | N/A | N/A | 🔴 NO API. |
| Temple Registration | ReviewSubmit (Step 4) | "Submit Registration" button | `navigate('/temple-registration/success')` | N/A | N/A | N/A | Does NOT call any API. Immediately navigates to success page. The entire collected form data is discarded. | N/A | N/A | 🔴 NO API. **Critical gap**: Backend `POST /api/v1/temples` and `POST /api/v1/temples/step` endpoints exist with full business logic (credential generation, email dispatch, MongoDB transactions) but are entirely unused. |
| Temple Registration | ReviewSubmit (Step 4) | "Edit" links (Temple/President/VP sections) | `navigate('/temple-registration')` etc. | N/A | N/A | N/A | Client-side navigation back to earlier steps | N/A | N/A | 🔴 NO API. |
| Temple Registration | RegistrationSuccess | "Go to Login" button | `navigate('/login')` | N/A | N/A | N/A | Navigation only | N/A | N/A | 🔴 NO API. |

---

## Summary of Mismatches & Gaps

### ⚠️ Frontend → Non-Existent Backend Endpoints

| Service Function | Called Endpoint | Status | Notes |
|---|---|---|---|
| `authService.forgotPassword()` | `POST /auth/forgot-password` | Route missing in backend | Used in `authService.ts` but ForgotPasswordPage uses `setTimeout` simulation instead |
| `adminService.adminGetExams()` | `GET /exams` | Route missing | Only `GET /exams/course/:courseId` exists |
| `adminService.adminCreateExam()` | `POST /exams` | Route missing | No admin exam creation endpoint in backend |
| `adminService.adminUpdateExam()` | `PUT /exams/:id` | Route missing | No exam update endpoint in backend |
| `adminService.adminDeleteExam()` | `DELETE /exams/:id` | Route missing | No exam deletion endpoint in backend |

### 🔴 Frontend UI With No API Calls (Mock or Simulated)

| Screen | Issue |
|---|---|
| `ForgotPasswordPage` | "Send Reset Link" uses `setTimeout` only — no real API call |
| `CoursesPage` (public) | Uses `getMockCourses()` — `getCourses()` service function with real API exists but unused |
| `CourseDetailPage` (public) | Uses `getMockCourses().find()` — `getCourseById()` service function exists but unused |
| Temple Registration (all 4 steps) | Zero API calls. Full backend temple registration API (`POST /temples`, `POST /temples/step`, OTP endpoints) completely disconnected from frontend |
| Temple "Verify Mobile" | Local state toggle only — OTP backend (`POST /otp/send`, `POST /otp/verify`) completely disconnected |

### ✅ Backend Endpoints Not Yet Mapped to Any UI

| Endpoint | Description |
|---|---|
| `GET /api/v1/temple-users/check-duplicate` | Check if temple user email/mobile/PAN already exists — no frontend usage found |
| `POST /api/v1/temples` | Full temple registration with transaction — no frontend call |
| `POST /api/v1/temples/step` | Step-by-step temple registration save — no frontend call |
| `GET /api/v1/temples/:id` | Get temple by ID — no frontend call |
| `PATCH /api/v1/temples/:id/status` | Approve/reject temple — no frontend call |
| `POST /api/v1/otp/send` | Send OTP to mobile — no frontend call |
| `POST /api/v1/otp/verify` | Verify OTP — no frontend call |
| `POST /api/v1/upload` | Upload file (logo/photo/id_card) — no frontend call in temple flow |
